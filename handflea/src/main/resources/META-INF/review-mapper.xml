<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ReviewMapper"> 

	<insert id="write" parameterType="kr.co.handflea.review.ReviewDTO">
	insert into review (mem_no, order_no, review_contents, review_photo, review_photopath, star_point, reg_date)
	values(#{mem_no}, #{order_no}, #{review_contents}, #{review_photo}, #{review_photopath}, #{star_point}, now());
	</insert>

	<select id="list">
	select board_no, writer, contents, write_date from review
	</select>

</mapper>