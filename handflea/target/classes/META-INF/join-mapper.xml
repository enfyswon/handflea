<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="JoinMapper">

	<insert mem_email="join" parameterType="kr.co.handflea.util.dto.MemberDTO">
	INSERT INTO member ( mem_name, mem_email, mem_pwd, mem_add_1, mem_add_2, mem_add_3, Mem_no )
	VALUES ( #{ mem_email}, #{mem_pwd}, now()
		,#{service_agree}, #{info_agree}, #{sms_agree}, #{mem_email_agree}
		,#{post_code}, #{mem_add_1}, #{mem_add_2}, #{mem_add_3} )
	</insert>

	<select mem_email="mem_emailCheck" parameterType="java.lang.String" resultType="int">
	select count(mem_email) cnt
	from member
	where mem_email = #{mem_email}
	</select>

</mapper>
