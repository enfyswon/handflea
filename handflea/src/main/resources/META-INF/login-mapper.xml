<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="LoginMapper">

	<select id="login" parameterType="kr.co.handflea.util.dto.MemberDTO"
						resultType="kr.co.handflea.util.dto.MemberDTO">
	select mem_no, mem_name, mem_email, mem_pwd, pnum, post_code, add_1, add_2, mem_photo, mem_photopath, bank_no, account_no, seller_yn
	from member
	where mem_email = #{mem_email} and mem_pwd = #{mem_pwd}
	</select>

</mapper>
